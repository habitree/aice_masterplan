# 05-01. 데이터 시각화

## 📋 목차

- [1. 데이터 시각화란?](#1-데이터-시각화란)
- [2. Matplotlib 기초](#2-matplotlib-기초)
- [3. Seaborn 기초](#3-seaborn-기초)
- [4. 주요 그래프 유형](#4-주요-그래프-유형)
- [5. 실습](#5-실습)
- [6. 요약](#6-요약)

---

## 1. 데이터 시각화란?

**데이터 시각화**는 데이터를 그래프나 차트로 표현하여 패턴, 트렌드, 관계를 시각적으로 이해하기 쉽게 만드는 과정입니다.

### 1.1 시각화의 장점

- **패턴 발견**: 데이터의 숨겨진 패턴 발견
- **의사소통**: 복잡한 데이터를 쉽게 전달
- **인사이트**: 빠른 데이터 이해 및 의사결정

---

## 2. Matplotlib 기초

### 2.1 기본 사용법

```python
import matplotlib.pyplot as plt
import numpy as np

# 기본 그래프
x = np.linspace(0, 10, 100)
y = np.sin(x)

plt.figure(figsize=(10, 6))
plt.plot(x, y)
plt.xlabel('X축')
plt.ylabel('Y축')
plt.title('Sine 그래프')
plt.grid(True)
plt.show()
```

### 2.2 여러 그래프 그리기

```python
import matplotlib.pyplot as plt
import numpy as np

x = np.linspace(0, 10, 100)
y1 = np.sin(x)
y2 = np.cos(x)

plt.figure(figsize=(10, 6))
plt.plot(x, y1, label='sin(x)')
plt.plot(x, y2, label='cos(x)')
plt.xlabel('X축')
plt.ylabel('Y축')
plt.title('삼각함수 그래프')
plt.legend()
plt.grid(True)
plt.show()
```

### 2.3 주요 그래프 유형

```python
import matplotlib.pyplot as plt
import numpy as np

# 막대 그래프
categories = ['A', 'B', 'C', 'D']
values = [10, 20, 15, 25]
plt.bar(categories, values)
plt.show()

# 산점도
x = np.random.randn(100)
y = np.random.randn(100)
plt.scatter(x, y)
plt.show()

# 히스토그램
data = np.random.randn(1000)
plt.hist(data, bins=30)
plt.show()

# 박스 플롯
data = [np.random.randn(100), np.random.randn(100)]
plt.boxplot(data)
plt.show()
```

---

## 3. Seaborn 기초

### 3.1 기본 사용법

```python
import seaborn as sns
import pandas as pd
import matplotlib.pyplot as plt

# Seaborn 스타일 설정
sns.set_style("darkgrid")

# 샘플 데이터
df = pd.DataFrame({
    'x': range(10),
    'y': range(10, 20)
})

sns.lineplot(data=df, x='x', y='y')
plt.show()
```

### 3.2 주요 그래프

```python
import seaborn as sns
import pandas as pd

# 데이터 로드
df = pd.read_csv('data.csv')

# 히스토그램
sns.histplot(data=df, x='나이', bins=20)

# 박스 플롯
sns.boxplot(data=df, x='직업', y='급여')

# 산점도
sns.scatterplot(data=df, x='나이', y='급여', hue='직업')

# 히트맵
correlation = df.corr()
sns.heatmap(correlation, annot=True)
```

---

## 4. 주요 그래프 유형

### 4.1 막대 그래프

```python
import matplotlib.pyplot as plt

categories = ['A', 'B', 'C', 'D']
values = [10, 20, 15, 25]

plt.bar(categories, values)
plt.xlabel('카테고리')
plt.ylabel('값')
plt.title('막대 그래프')
plt.show()
```

### 4.2 선 그래프

```python
import matplotlib.pyplot as plt
import numpy as np

x = np.linspace(0, 10, 100)
y = np.sin(x)

plt.plot(x, y)
plt.xlabel('X')
plt.ylabel('Y')
plt.title('선 그래프')
plt.show()
```

### 4.3 산점도

```python
import matplotlib.pyplot as plt
import numpy as np

x = np.random.randn(100)
y = np.random.randn(100)

plt.scatter(x, y, alpha=0.5)
plt.xlabel('X')
plt.ylabel('Y')
plt.title('산점도')
plt.show()
```

---

## 5. 실습

### 실습: 데이터 시각화 종합

```python
import pandas as pd
import matplotlib.pyplot as plt
import seaborn as sns

# 데이터 생성
data = {
    '이름': ['홍길동', '김철수', '이영희', '박민수'],
    '나이': [25, 30, 28, 35],
    '급여': [3000, 4000, 3500, 5000],
    '직업': ['학생', '회사원', '교사', '의사']
}
df = pd.DataFrame(data)

# 1. 막대 그래프
plt.figure(figsize=(10, 6))
plt.bar(df['이름'], df['급여'])
plt.xlabel('이름')
plt.ylabel('급여')
plt.title('직원별 급여')
plt.show()

# 2. 산점도
plt.figure(figsize=(10, 6))
plt.scatter(df['나이'], df['급여'])
plt.xlabel('나이')
plt.ylabel('급여')
plt.title('나이 vs 급여')
plt.show()
```

---

## 6. 요약

### 핵심 정리

1. **Matplotlib**: 기본 시각화 라이브러리
2. **Seaborn**: 통계 시각화에 특화
3. **주요 그래프**: 막대, 선, 산점도, 히스토그램, 박스 플롯

### AICE 실전 팁

- **그래프 선택**: 데이터 특성에 맞는 그래프 선택
- **레이블**: xlabel, ylabel, title 필수
- **스타일**: 일관된 스타일 사용

---

## ✅ 체크리스트

- [ ] Matplotlib 기본 사용법 이해
- [ ] Seaborn 기본 사용법 이해
- [ ] 주요 그래프 유형 숙지
- [ ] 실습 코드 작성 및 실행 완료

---

## 📚 참고 자료

- [Matplotlib 공식 문서](https://matplotlib.org/)
- [Seaborn 공식 문서](https://seaborn.pydata.org/)

---

**작성일**: 2025-11-19  
**이전 학습**: [04_01_데이터전처리](../04_전처리/04_01_데이터전처리.md)  
**다음 학습**: [06_01_머신러닝기초](../06_머신러닝/06_01_머신러닝기초.md)

