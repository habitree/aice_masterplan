# 02-04. 파이썬 함수와 모듈

## 📋 목차

- [1. 함수 정의와 호출](#1-함수-정의와-호출)
- [2. 매개변수와 인자](#2-매개변수와-인자)
- [3. 반환값 (return)](#3-반환값-return)
- [4. 람다 함수](#4-람다-함수)
- [5. 모듈과 패키지](#5-모듈과-패키지)
- [6. 내장 함수](#6-내장-함수)
- [7. 실습](#7-실습)
- [8. 요약](#8-요약)

---

## 1. 함수 정의와 호출

### 1.1 기본 함수 정의

```python
# 함수 정의
def greet():
    print("Hello, AICE!")

# 함수 호출
greet()  # Hello, AICE!
```

### 1.2 매개변수가 있는 함수

```python
def greet(name):
    print(f"Hello, {name}!")

greet("홍길동")  # Hello, 홍길동!
```

### 1.3 여러 매개변수

```python
def add(a, b):
    result = a + b
    print(f"{a} + {b} = {result}")

add(3, 5)  # 3 + 5 = 8
```

---

## 2. 매개변수와 인자

### 2.1 위치 인자

```python
def introduce(name, age, city):
    print(f"이름: {name}, 나이: {age}, 도시: {city}")

introduce("홍길동", 25, "서울")
```

### 2.2 키워드 인자

```python
# 순서 상관없이 키워드로 전달
introduce(age=25, city="서울", name="홍길동")
```

### 2.3 기본값 매개변수

```python
def greet(name, greeting="안녕하세요"):
    print(f"{greeting}, {name}님!")

greet("홍길동")              # 안녕하세요, 홍길동님!
greet("이영희", "반갑습니다")  # 반갑습니다, 이영희님!
```

### 2.4 가변 인자 (*args)

```python
def sum_all(*args):
    total = 0
    for num in args:
        total += num
    return total

print(sum_all(1, 2, 3))        # 6
print(sum_all(1, 2, 3, 4, 5))  # 15
```

### 2.5 키워드 가변 인자 (**kwargs)

```python
def print_info(**kwargs):
    for key, value in kwargs.items():
        print(f"{key}: {value}")

print_info(name="홍길동", age=25, city="서울")
# name: 홍길동
# age: 25
# city: 서울
```

---

## 3. 반환값 (return)

### 3.1 기본 return

```python
def add(a, b):
    return a + b

result = add(3, 5)
print(result)  # 8
```

### 3.2 여러 값 반환

```python
def calculate(a, b):
    return a + b, a - b, a * b, a / b

sum_result, diff, prod, quot = calculate(10, 3)
print(f"합: {sum_result}, 차: {diff}, 곱: {prod}, 몫: {quot}")
```

### 3.3 조건부 return

```python
def get_grade(score):
    if score >= 90:
        return "A"
    elif score >= 80:
        return "B"
    elif score >= 70:
        return "C"
    else:
        return "F"

print(get_grade(85))  # B
```

---

## 4. 람다 함수

### 4.1 람다 기본

```python
# 일반 함수
def square(x):
    return x ** 2

# 람다 함수
square_lambda = lambda x: x ** 2

print(square(5))          # 25
print(square_lambda(5))   # 25
```

### 4.2 람다 활용

```python
# map()과 함께 사용
numbers = [1, 2, 3, 4, 5]
squared = list(map(lambda x: x ** 2, numbers))
print(squared)  # [1, 4, 9, 16, 25]

# filter()와 함께 사용
evens = list(filter(lambda x: x % 2 == 0, numbers))
print(evens)  # [2, 4]

# sorted()와 함께 사용
students = [("홍길동", 25), ("이영희", 23), ("김철수", 27)]
sorted_by_age = sorted(students, key=lambda x: x[1])
print(sorted_by_age)  # [('이영희', 23), ('홍길동', 25), ('김철수', 27)]
```

---

## 5. 모듈과 패키지

### 5.1 모듈 import

```python
# 전체 모듈 import
import math
print(math.pi)  # 3.141592653589793

# 특정 함수만 import
from math import sqrt, pow
print(sqrt(16))  # 4.0

# 별칭 사용
import numpy as np
import pandas as pd
```

### 5.2 자체 모듈 생성

```python
# my_module.py 파일 생성
# def greet(name):
#     return f"Hello, {name}!"
#
# def add(a, b):
#     return a + b

# 다른 파일에서 사용
# import my_module
# print(my_module.greet("홍길동"))
```

### 5.3 패키지

```python
# 패키지 구조
# my_package/
#     __init__.py
#     module1.py
#     module2.py

# 사용
# from my_package import module1
# import my_package.module2
```

---

## 6. 내장 함수

### 6.1 수학 함수

```python
# 절댓값
print(abs(-5))  # 5

# 최댓값, 최솟값
print(max(1, 2, 3, 4, 5))  # 5
print(min(1, 2, 3, 4, 5))  # 1

# 합계
print(sum([1, 2, 3, 4, 5]))  # 15

# 반올림
print(round(3.14159, 2))  # 3.14
```

### 6.2 타입 변환

```python
# 정수 변환
print(int(3.7))      # 3
print(int("123"))    # 123

# 실수 변환
print(float(5))      # 5.0
print(float("3.14")) # 3.14

# 문자열 변환
print(str(123))      # "123"

# 리스트 변환
print(list("hello")) # ['h', 'e', 'l', 'l', 'o']
```

### 6.3 유용한 내장 함수

```python
# 길이
print(len("hello"))     # 5
print(len([1, 2, 3]))   # 3

# 타입 확인
print(type(10))         # <class 'int'>
print(isinstance(10, int))  # True

# 범위
print(list(range(5)))   # [0, 1, 2, 3, 4]

# 열거
for i, value in enumerate(["a", "b", "c"]):
    print(i, value)     # 0 a, 1 b, 2 c

# zip
names = ["홍길동", "이영희"]
ages = [25, 23]
for name, age in zip(names, ages):
    print(name, age)    # 홍길동 25, 이영희 23
```

---

## 7. 실습

### 실습 1: 통계 함수 만들기

```python
def calculate_stats(numbers):
    """숫자 리스트의 통계를 계산하는 함수"""
    if not numbers:
        return None
    
    return {
        "합계": sum(numbers),
        "평균": sum(numbers) / len(numbers),
        "최댓값": max(numbers),
        "최솟값": min(numbers),
        "개수": len(numbers)
    }

scores = [85, 92, 78, 96, 88, 75, 90]
stats = calculate_stats(scores)

for key, value in stats.items():
    if isinstance(value, float):
        print(f"{key}: {value:.2f}")
    else:
        print(f"{key}: {value}")
```

### 실습 2: 데이터 필터링 함수

```python
def filter_data(data, condition_func):
    """조건 함수에 맞는 데이터만 필터링"""
    return [item for item in data if condition_func(item)]

# 사용 예시
numbers = [23, 45, 12, 67, 34, 89, 56, 23, 12, 45]

# 50 이상인 숫자만
large_numbers = filter_data(numbers, lambda x: x >= 50)
print(f"50 이상: {large_numbers}")

# 짝수만
evens = filter_data(numbers, lambda x: x % 2 == 0)
print(f"짝수: {evens}")
```

### 실습 3: 학생 관리 시스템

```python
def add_student(students, name, age, grade):
    """학생 추가"""
    students.append({"name": name, "age": age, "grade": grade})

def find_student(students, name):
    """학생 찾기"""
    for student in students:
        if student["name"] == name:
            return student
    return None

def get_average_age(students):
    """평균 나이 계산"""
    if not students:
        return 0
    total_age = sum(student["age"] for student in students)
    return total_age / len(students)

# 사용
students = []
add_student(students, "홍길동", 25, "A")
add_student(students, "이영희", 23, "B")
add_student(students, "김철수", 27, "A")

print(find_student(students, "홍길동"))
print(f"평균 나이: {get_average_age(students):.1f}세")
```

---

## 8. 요약

### 핵심 정리

1. **함수 정의**: `def` 키워드 사용
2. **매개변수**: 위치 인자, 키워드 인자, 기본값, 가변 인자
3. **반환값**: `return`으로 값 반환 (여러 값 가능)
4. **람다**: 간단한 함수를 한 줄로 표현
5. **모듈**: 코드 재사용을 위한 단위
6. **내장 함수**: 파이썬에서 제공하는 유용한 함수들

### AICE 실전 팁

- **함수명**: 동사로 시작, 의미 명확하게
- **docstring**: 함수 설명을 문자열로 작성
- **모듈화**: 재사용 가능한 코드는 함수로 분리
- **내장 함수**: 이미 구현된 함수 활용

> 💡 **Tip:** 함수는 한 가지 일만 잘하도록 설계하는 것이 좋습니다 (단일 책임 원칙).

---

## ✅ 체크리스트

- [ ] 함수 정의 및 호출 방법 이해
- [ ] 다양한 매개변수 타입 이해
- [ ] return 문으로 값 반환
- [ ] 람다 함수 작성 및 활용
- [ ] 모듈 import 및 사용
- [ ] 주요 내장 함수 숙지
- [ ] 실습 코드 작성 및 실행 완료

---

## 📚 참고 자료

- [Python 공식 문서 - 함수](https://docs.python.org/ko/3/tutorial/controlflow.html#defining-functions)
- [Python 공식 문서 - 모듈](https://docs.python.org/ko/3/tutorial/modules.html)

---

**작성일**: 2025-11-18  
**다음 학습**: 02_05_웹데이터수집.md

