# 02-03. 파이썬 제어문

## 📋 목차

- [1. 조건문 (if)](#1-조건문-if)
- [2. 반복문 (for, while)](#2-반복문-for-while)
- [3. 제어문 활용](#3-제어문-활용)
- [4. 실습](#4-실습)
- [5. 요약](#5-요약)

---

## 1. 조건문 (if)

### 1.1 기본 if 문

```python
# 기본 구조
age = 20

if age >= 18:
    print("성인입니다")
```

### 1.2 if-else 문

```python
age = 15

if age >= 18:
    print("성인입니다")
else:
    print("미성년자입니다")
```

### 1.3 if-elif-else 문

```python
score = 85

if score >= 90:
    grade = "A"
elif score >= 80:
    grade = "B"
elif score >= 70:
    grade = "C"
else:
    grade = "F"

print(f"학점: {grade}")
```

### 1.4 중첩 if 문

```python
age = 25
has_license = True

if age >= 18:
    if has_license:
        print("운전 가능합니다")
    else:
        print("면허가 필요합니다")
else:
    print("미성년자는 운전할 수 없습니다")
```

### 1.5 조건식 (삼항 연산자)

```python
# 기본 문법: 값1 if 조건 else 값2
age = 20
status = "성인" if age >= 18 else "미성년자"
print(status)  # 성인

# 활용 예시
score = 85
result = "합격" if score >= 80 else "불합격"
print(result)  # 합격
```

---

## 2. 반복문 (for, while)

### 2.1 for 문

```python
# 리스트 순회
fruits = ["apple", "banana", "orange"]
for fruit in fruits:
    print(fruit)

# range() 사용
for i in range(5):
    print(i)  # 0, 1, 2, 3, 4

for i in range(1, 6):
    print(i)  # 1, 2, 3, 4, 5

for i in range(0, 10, 2):
    print(i)  # 0, 2, 4, 6, 8
```

### 2.2 enumerate() - 인덱스와 값 함께

```python
fruits = ["apple", "banana", "orange"]

for index, fruit in enumerate(fruits):
    print(f"{index}: {fruit}")

# 출력:
# 0: apple
# 1: banana
# 2: orange
```

### 2.3 딕셔너리 순회

```python
student = {"name": "홍길동", "age": 25, "grade": "A"}

# 키만 순회
for key in student:
    print(key)

# 키-값 쌍 순회
for key, value in student.items():
    print(f"{key}: {value}")

# 값만 순회
for value in student.values():
    print(value)
```

### 2.4 while 문

```python
# 기본 while 문
count = 0
while count < 5:
    print(count)
    count += 1

# 무한 루프 (주의!)
# while True:
#     print("무한 반복")
```

### 2.5 break와 continue

```python
# break: 반복문 종료
for i in range(10):
    if i == 5:
        break
    print(i)  # 0, 1, 2, 3, 4

# continue: 다음 반복으로 건너뛰기
for i in range(10):
    if i % 2 == 0:
        continue
    print(i)  # 1, 3, 5, 7, 9
```

### 2.6 else 절 (반복문)

```python
# for-else: break 없이 정상 종료 시 실행
for i in range(5):
    print(i)
else:
    print("반복 완료")

# while-else
count = 0
while count < 5:
    print(count)
    count += 1
else:
    print("반복 완료")
```

### 2.7 중첩 반복문

```python
# 구구단 출력
for i in range(2, 10):
    for j in range(1, 10):
        print(f"{i} x {j} = {i*j}")
    print()  # 줄바꿈
```

---

## 3. 제어문 활용

### 3.1 리스트 컴프리헨션과 조건문

```python
# 조건문 포함
numbers = [x for x in range(1, 11) if x % 2 == 0]
print(numbers)  # [2, 4, 6, 8, 10]

# if-else 포함
result = ["짝수" if x % 2 == 0 else "홀수" for x in range(1, 6)]
print(result)  # ['홀수', '짝수', '홀수', '짝수', '홀수']
```

### 3.2 pass 문

```python
# 아무것도 하지 않지만 문법상 필요할 때
if True:
    pass  # 나중에 구현할 예정

# 빈 함수나 클래스 정의 시
def my_function():
    pass
```

---

## 4. 실습

### 실습 1: 성적 등급 계산기

```python
# 여러 학생의 성적 처리
students = [
    {"name": "홍길동", "score": 95},
    {"name": "이영희", "score": 87},
    {"name": "김철수", "score": 72},
    {"name": "박민수", "score": 65}
]

for student in students:
    score = student["score"]
    if score >= 90:
        grade = "A"
    elif score >= 80:
        grade = "B"
    elif score >= 70:
        grade = "C"
    elif score >= 60:
        grade = "D"
    else:
        grade = "F"
    
    print(f"{student['name']}: {score}점 → {grade}등급")
```

### 실습 2: 숫자 게임

```python
import random

# 1부터 100까지 랜덤 숫자 생성
target = random.randint(1, 100)
attempts = 0
max_attempts = 7

print("1부터 100까지 숫자를 맞춰보세요!")

while attempts < max_attempts:
    guess = int(input(f"시도 {attempts + 1}/{max_attempts}: "))
    attempts += 1
    
    if guess == target:
        print(f"정답입니다! {attempts}번 만에 맞췄습니다.")
        break
    elif guess < target:
        print("더 큰 수입니다.")
    else:
        print("더 작은 수입니다.")
else:
    print(f"실패했습니다. 정답은 {target}이었습니다.")
```

### 실습 3: 데이터 필터링

```python
# 숫자 리스트에서 조건에 맞는 데이터 추출
numbers = [23, 45, 12, 67, 34, 89, 56, 23, 12, 45]

# 50 이상인 숫자만 추출
filtered = [x for x in numbers if x >= 50]
print(f"50 이상: {filtered}")

# 짝수만 추출
evens = [x for x in numbers if x % 2 == 0]
print(f"짝수: {evens}")

# 중복 제거 후 정렬
unique_sorted = sorted(set(numbers))
print(f"중복 제거 후 정렬: {unique_sorted}")
```

### 실습 4: 통계 계산

```python
# 점수 리스트
scores = [85, 92, 78, 96, 88, 75, 90, 82, 95, 87]

# 통계 계산
total = 0
count = 0
max_score = scores[0]
min_score = scores[0]

for score in scores:
    total += score
    count += 1
    if score > max_score:
        max_score = score
    if score < min_score:
        min_score = score

average = total / count

print(f"총점: {total}")
print(f"평균: {average:.2f}")
print(f"최댓값: {max_score}")
print(f"최솟값: {min_score}")
```

---

## 5. 요약

### 핵심 정리

1. **조건문**: if, elif, else로 분기 처리
2. **반복문**: for (정해진 횟수), while (조건 만족 시)
3. **제어 키워드**: break (종료), continue (건너뛰기)
4. **리스트 컴프리헨션**: 간결한 반복문 표현

### AICE 실전 팁

- **들여쓰기**: 파이썬은 들여쓰기로 블록 구분 (4칸 권장)
- **range()**: 반복 횟수 제어에 유용
- **enumerate()**: 인덱스와 값이 모두 필요할 때
- **리스트 컴프리헨션**: 간결하고 빠른 리스트 생성

> ⚠️ **Warning:** 무한 루프에 주의하세요. while True 사용 시 반드시 break 조건을 확인하세요.

---

## ✅ 체크리스트

- [ ] if-elif-else 문 이해 및 활용
- [ ] for 문으로 리스트, 딕셔너리 순회
- [ ] while 문으로 조건 반복 처리
- [ ] break, continue 사용법 숙지
- [ ] 중첩 반복문 이해
- [ ] 리스트 컴프리헨션 활용
- [ ] 실습 코드 작성 및 실행 완료

---

## 📚 참고 자료

- [Python 공식 문서 - 제어 흐름](https://docs.python.org/ko/3/tutorial/controlflow.html)

---

**작성일**: 2025-11-18  
**다음 학습**: 02_04_함수모듈.md

